apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  selector:
    app: myapp
    version: blue   # 👈 initially points to blue
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer